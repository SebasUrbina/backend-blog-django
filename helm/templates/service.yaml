apiVersion: v1
kind: Service
metadata:
  name: {{ include "django-blog.fullname" . }}
  labels:
    {{- include "django-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "django-blog.selectorLabels" . | nindent 4 }}
